async function loadLiveDashboard(){try{const stats=await (await fetch("./data/stats.json?t="+Date.now())).json();const cards=document.querySelectorAll(".stats-panel article b");if(cards[0]) cards[0].textContent=(stats.today||0)+"건";if(cards[1]) cards[1].textContent="+"+Number(stats.avg_edge||0).toFixed(2)+"%";if(cards[2]) cards[2].textContent=(stats.vip||0)+"건";if(cards[3]) cards[3].textContent="+"+Number(stats.max_edge||0).toFixed(2)+"%";const rows=await (await fetch("./data/signals.json?t="+Date.now())).json();if(rows.length){const last=rows[0];const live=document.querySelector(".live-edge-card");live.innerHTML=`<div class="live-head"><span>LIVE EDGE</span><b>+${Number(last.real_edge).toFixed(2)}%</b></div><h3>🚨 ${last.coin} 양방 후보</h3><p>국내: ${last.domestic_exchange} · 해외선물: ${last.foreign_exchange}</p>`;}}catch(e){console.log(e)}}document.addEventListener("DOMContentLoaded",()=>{loadLiveDashboard();setInterval(loadLiveDashboard,10000);});